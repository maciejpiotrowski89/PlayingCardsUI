import XCTest
@testable import PlayingCardsUI

final class PlayingCardsUITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PlayingCardsUI().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
